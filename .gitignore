__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
.ty_cache/
*.egg-info/
dist/
build/
.DS_Store
.claude/
output/
# NiceGUI's per-user storage, written under the working directory by a frontend run from the tree.
.nicegui/

# Transient release artifact: dev-tools' generate-changelog writes this for
# `gh release create --notes-file`; it's regenerated every release, never committed.
release-body.md
